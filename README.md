This is the readme for the dotfiles bare repository.

# installing dotfiles

pull this as a dotfile and set this alias in your shell

```bash
alias dgit='/usr/bin/git --git-dir=$HOME/.dotfiles --work-tree=$HOME'
```
